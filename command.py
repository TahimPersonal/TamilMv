# command.py
def handle_start(message):
    bot.send_message(message.chat.id, "Welcome! This bot fetches latest posts.")

def handle_latest(message):
    # Logic to fetch and send the latest post magnet links
    pass

def handle_buttons(message):
    # Logic to handle buttons (if needed in the future)
    pass
